/**
 * Feature-internal pool generators for {@link app.zoneblitz.league.hiring}. Holds the {@code
 * HeadCoach}, {@code DirectorOfScouting}, {@code Coordinator}, {@code PositionCoach}, and {@code
 * ScoutCandidate} generators; the per-kind market-band files; and the {@code
 * StaffPreferencesFactory}. Generators obey the hidden-info contract documented on {@code
 * CandidateGenerator}.
 *
 * <p>See the hiring feature's {@code README.md} in the parent directory for the full layout.
 */
package app.zoneblitz.league.hiring.generation;
