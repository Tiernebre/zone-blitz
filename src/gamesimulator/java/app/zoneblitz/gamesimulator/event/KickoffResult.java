package app.zoneblitz.gamesimulator.event;

/** Outcome classification of a kickoff. */
public enum KickoffResult {
  RETURNED,
  FAIR_CATCH,
  TOUCHBACK,
  OUT_OF_BOUNDS,
  ONSIDE_RECOVERED_BY_KICKING,
  ONSIDE_RECOVERED_BY_RECEIVING
}
